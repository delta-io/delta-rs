[workspace]
members = [
    "ruby",
    "rust",
    "python",
    "glibc_version",
    "aws/delta-checkpoint",
    "dynamodb_lock",
]
exclude = ["proofs", "delta-inspect"]

[profile.dev]
split-debuginfo = "unpacked"

[profile.release-with-debug]
inherits = "release"
debug = true
