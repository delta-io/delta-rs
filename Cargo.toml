[workspace]
members = [
    "rust",
    "python",
    "glibc_version",
    "aws/delta-checkpoint",
    "distributed_lock",
]
exclude = ["proofs", "delta-inspect"]

[profile.release-with-debug]
inherits = "release"
debug = true
