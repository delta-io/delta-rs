* track tombstones
* check crc
* replace all unwrap with Result
* support write, currently blocked on adding write support to parquet crate
* make all IO async
* handle commitInfo action
* handle dataChange field
* use list api to find transaction logs to apply
* time travel by time
* prefetch log content in parallel
